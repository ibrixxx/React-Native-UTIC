import React from 'react'
//import {Router} from '@react-navigation/native'
import Sidebar from "./components/Sidebar";
//import {Stack, Scene} from "react-native-router-flux";

export default function Routes() {
    return (
        <></>
    );

    //Javlja error kada je ovo ispod u return-u
    /*
        <Router>
            <Stack key="root">
                <Scene key="home" component={Sidebar} title="Login" />
                <Scene key="user" component={Sidebar} title="Register" />
            </Stack>
        </Router>*/
}